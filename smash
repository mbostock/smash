#!/usr/bin/env node

var smash = require("./");

smash(process.argv.slice(2))
    .pipe(process.stdout)
    .on("error", function() { console.warn("error"); })
    .on("end", function() { console.warn("end"); });
